import React, { useEffect, useState } from 'react';

import styles from './Assets.module.scss';

type Props = {};

const AssetsView = (props: Props) => {
  return <div className={styles.container}>Assets</div>;
};

export default AssetsView;

import React, { useEffect, useState } from 'react';

import styles from './Batches.module.scss';

type Props = {};

const BatchesView = (props: Props) => {
  return <div className={styles.container}>Batches</div>;
};

export default BatchesView;

import React, { useEffect, useState } from 'react';

import styles from './Scheduled.module.scss';

type Props = {};

const ScheduledView = (props: Props) => {
  return <div className={styles.container}>Scheduled</div>;
};

export default ScheduledView;

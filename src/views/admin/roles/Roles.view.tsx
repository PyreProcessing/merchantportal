import React, { useEffect, useState } from 'react';

import styles from './Roles.module.scss';

type Props = {};

const RolesView = (props: Props) => {
  return <div className={styles.container}>Roles</div>;
};

export default RolesView;

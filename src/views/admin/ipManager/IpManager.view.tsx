import React, { useEffect, useState } from 'react';

import styles from './IpManager.module.scss';

type Props = {};

const IPManagerView = (props: Props) => {
  return <div className={styles.container}>IPManager</div>;
};

export default IPManagerView;

import React, { useEffect, useState } from 'react';

import styles from './CustomImport.module.scss';

type Props = {};

const CustomImportView = (props: Props) => {
  return <div className={styles.container}>CustomImport</div>;
};

export default CustomImportView;
